if (typeof define !== 'function') { var define = require('amdefine')(module) }

define(['log'],
function(log)
{
	var adaptor = {
	storeData: function (name, collection)
	{
		
	}};
	return adaptor;
});